apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sso-server.fullname" . }}-test-connection"
  labels:
    {{- include "sso-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sso-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
